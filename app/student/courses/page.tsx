"use client";

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function StudentCoursesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>My Courses</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for student courses.</p>
      </CardContent>
    </Card>
  );
}
